{
  "_comment": "Copy to credentials.json beside master.config.json (gitignored). Portal users + OC login — keep out of git.",
  "_comment_dual_ocp": "Two OCP clusters: use oc.loginServers in master (context→API URL) + either shared loginUser/loginPassword here, or loginCredentials per context: { \"cwdc\": { \"user\": \"...\", \"password\": \"...\" }, \"tls2\": { ... } }",
  "users": {
    "admin": "CHANGE_ME_OR_USE_HASH"
  },
  "oc": {
    "ocAutoLogin": true,
    "loginUser": "ocpadmin",
    "loginPassword": "REPLACE_ME",
    "loginServers": {},
    "loginTokens": {}
  }
}
